// This is an example of a simple Rust function that takes two numbers as arguments and returns their sum.
fn add(a: i32, b: i32) -> i32 {
    a + b
}

// You can use this function like so:
let result = add(4, 5); // result is 9
